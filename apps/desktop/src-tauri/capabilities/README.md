# Tauri capabilities

Capabilities define which plugin/core commands the webview is allowed to invoke.
The main window uses `default.json`. Add new capability files per-window as the
app grows (e.g. a `preferences.json` for the Preferences window).
