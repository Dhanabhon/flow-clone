/**
 * verification feature — the post-clone verification screen.
 *
 * After the clone finishes the app transitions automatically into verification,
 * showing a separate shield-icon progress indicator. Result data comes from
 * the `verifying` / `completed` phases of the Progress stream.
 */
export {};
