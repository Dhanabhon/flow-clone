/**
 * report feature — the success screen and report export.
 *
 * Shows a green check, a summary card (source/target/elapsed/avg
 * speed/verification), and Export Report (Markdown / JSON) buttons. Report
 * generation lives in flowclone-report; the desktop app triggers it via a
 * Tauri command and saves the file.
 */
export {};
