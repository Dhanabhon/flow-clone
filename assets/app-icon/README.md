# App icon source

Production app icons live here (`1024x1024.png` source). The Tauri build
consumes pre-rendered sizes from `apps/desktop/src-tauri/icons/`.

Add the source PNG here; the icon set is generated with:
```
pnpm tauri icon assets/app-icon/source.png
```
